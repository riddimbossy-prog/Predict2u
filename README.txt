Predict2u Community + Games Phone Fix v146

UPLOAD/REPLACE THESE THREE FILES:
1. community.html
2. engines.html
3. sw.js

PHONE FIXES — COMMUNITY:
- Desktop navigation is hidden on phones.
- Compact logo and Community page chip.
- Smaller heading, description, cards, and controls.
- Board filters stack correctly.
- Picks, slips, members, leaderboard rows, and tabs stay inside the screen.
- Four-item bottom navigation added.
- Slip button sits above the bottom navigation.
- Community now registers sw.js.

PHONE FIXES — GAMES:
- Date tabs and engine tabs never shrink or overlap.
- Both rows scroll horizontally.
- Match cards remain inside the screen.
- Details control uses the available width.
- Slip button sits above the bottom navigation.
- Games item is highlighted in the bottom navigation.

TABLET AND DESKTOP:
- Existing layouts remain unchanged because all new layout rules use
  max-width: 639px.

CACHE:
- Service-worker version is predict2u-v146.
- After deployment, refresh the site once.
- If an old layout remains, close the browser tab and reopen it so the new
  service worker can take control.
