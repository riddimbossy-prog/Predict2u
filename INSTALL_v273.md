# Predict2U v273 — Position, PPG, Kickoff + Recent-5 Popup

Copy every file in the patch folder into the repository root and replace matching files.

Then commit/push, wait for deployment, and fully close/reopen the installed PWA so service-worker cache v273 activates.

No new secret, database migration, or API key is required.
