# Never commit a config.txt that contains a real API key.
# In the cloud, the workflow writes config.txt from GitHub Secrets,
# uses it, then discards it — it must never be saved to the repo.
# (We keep a SAFE template named config.example.txt instead.)
config.txt

# OS / editor junk
.DS_Store
Thumbs.db
node_modules/
