PREDICT2U — COMPLETE CURRENT BUNDLE (replace ALL of these)
===========================================================
This is EVERYTHING at its latest version. Replacing this whole set
guarantees nothing is left stale. Keep the folder structure.

--- REPO ROOT ---
  index.html            5 engines (Normal/Strict/Ultra/Elite/APEX),
                        per-engine dashboard, scores+flags+WON/LOST on all
                        lists, slip builder (market filter + 20 legs),
                        safe-odds filter, PWA wiring, analytics.
  banker-engine.js      All 5 engines incl. APEX (home-advantage layer);
                        Elite & Apex now PREFER REAL stats, fall back to
                        estimates only when real data is missing.
  fetch-data.js         Fetches REAL team stats (clean-sheet/FTS/win-rate)
                        for upcoming games, cached. Captures country/flag.
  manifest.webmanifest  PWA
  sw.js                 PWA service worker — CACHE_VERSION = v7
  icon-*.png (4)        app icons (your logo)
  apple-touch-icon.png  iOS icon

--- .github/workflows/ ---
  update-data.yml       rebase-safe fetcher + writes STATS secret

--- NOT IN ZIP ---
  logo.png   -> your header logo (upload your own)
  config.txt -> never commit (lives in GitHub Secrets)

===========================================================
DEPLOY:
  1. Upload ALL files above (one batch). Don't run fetcher mid-upload.
  2. (Optional) add a GitHub Secret named STATS = true. If absent, stats
     default to ON anyway.
  3. Actions -> Run workflow ONCE. THIS RUN IS SLOW (real stats fetch) —
     possibly 30-60 min. Let it finish; do not cancel.
  4. Hard-refresh (Ctrl+Shift+R). On phone PWA: uninstall + reinstall
     (CACHE_VERSION bumped to v7 forces the update).

VERIFY:
  - FIVE engine tabs incl. "Apex Bankers"
  - Elite/Apex picks show scores, flags, WON/LOST tags
  - On games with real stats, the amber "ESTIMATED stats" warning is GONE
  - Slip builder has market filter + up to 20 legs
